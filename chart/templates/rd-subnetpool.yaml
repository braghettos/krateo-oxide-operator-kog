{{- if .Values.restDefinitions.subnetpool.enabled }}
apiVersion: ogen.krateo.io/v1alpha1
kind: RestDefinition
metadata:
  name: {{ .Release.Name }}-subnetpool
  labels:
    {{- include "krateo-oxide-operator-kog.labels" . | nindent 4 }}
  {{- if .Values.verbose }}
  annotations:
    krateo.io/connector-verbose: "true"
  {{- end }}
spec:
  oasPath: configmap://{{ .Release.Namespace }}/{{ .Release.Name }}-subnetpool/subnetpool.yaml
  resourceGroup: {{ .Values.resourceGroup }}
  resource:
    kind: {{ .Values.restDefinitions.subnetpool.kind }}
    # OxideSubnetPool is addressed by its user-supplied `name` (the {pool} path param); `id` is
    # server-generated/read-only. Fleet/system-scoped resource (requires fleet-admin token).
    identifiers:
      - name
    additionalStatusFields:
      - id
    # The {pool} path placeholder would otherwise surface as a redundant spec field;
    # exclude it and source its value from spec.name via requestFieldMapping below.
    excludedSpecFields:
      - pool
    verbsDescription:
      - action: create
        method: POST
        path: /v1/system/subnet-pools
      - action: get
        method: GET
        path: /v1/system/subnet-pools/{pool}
        requestFieldMapping:
          - inPath: pool
            inCustomResource: spec.name
      - action: update
        method: PUT
        path: /v1/system/subnet-pools/{pool}
        requestFieldMapping:
          - inPath: pool
            inCustomResource: spec.name
      - action: delete
        method: DELETE
        path: /v1/system/subnet-pools/{pool}
        requestFieldMapping:
          - inPath: pool
            inCustomResource: spec.name
{{- end }}
