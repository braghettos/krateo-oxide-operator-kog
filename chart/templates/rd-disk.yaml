{{- if .Values.restDefinitions.disk.enabled }}
apiVersion: ogen.krateo.io/v1alpha1
kind: RestDefinition
metadata:
  name: {{ .Release.Name }}-disk
  labels:
    {{- include "krateo-oxide-operator-kog.labels" . | nindent 4 }}
  {{- if .Values.verbose }}
  annotations:
    krateo.io/connector-verbose: "true"
  {{- end }}
spec:
  oasPath: configmap://{{ .Release.Namespace }}/{{ .Release.Name }}-disk/disk.yaml
  resourceGroup: {{ .Values.resourceGroup }}
  resource:
    kind: {{ .Values.restDefinitions.disk.kind }}
    # OxideDisk is addressed by its user-supplied `name` (the {disk} path param); `id` is
    # server-generated/read-only. Project-scoped: the `project` query param is a spec field.
    identifiers:
      - name
    additionalStatusFields:
      - id
    # The {disk} path placeholder would otherwise surface as a redundant spec field;
    # exclude it and source its value from spec.name via requestFieldMapping below.
    excludedSpecFields:
      - disk
    verbsDescription:
      - action: create
        method: POST
        path: /v1/disks
      - action: get
        method: GET
        path: /v1/disks/{disk}
        requestFieldMapping:
          - inPath: disk
            inCustomResource: spec.name
      - action: delete
        method: DELETE
        path: /v1/disks/{disk}
        requestFieldMapping:
          - inPath: disk
            inCustomResource: spec.name
{{- end }}
