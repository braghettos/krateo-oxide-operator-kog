{{- if .Values.restDefinitions.affinitygroup.enabled }}
apiVersion: ogen.krateo.io/v1alpha1
kind: RestDefinition
metadata:
  name: {{ .Release.Name }}-affinitygroup
  labels:
    {{- include "krateo-oxide-operator-kog.labels" . | nindent 4 }}
  {{- if .Values.verbose }}
  annotations:
    krateo.io/connector-verbose: "true"
  {{- end }}
spec:
  oasPath: configmap://{{ .Release.Namespace }}/{{ .Release.Name }}-affinitygroup/affinitygroup.yaml
  resourceGroup: {{ .Values.resourceGroup }}
  resource:
    kind: {{ .Values.restDefinitions.affinitygroup.kind }}
    # OxideAffinityGroup is addressed by its user-supplied `name` (the {affinity_group} path param); `id` is
    # server-generated/read-only. Project-scoped: the `project` query param is a spec field.
    identifiers:
      - name
    additionalStatusFields:
      - id
    # The {affinity_group} path placeholder would otherwise surface as a redundant spec field;
    # exclude it and source its value from spec.name via requestFieldMapping below.
    excludedSpecFields:
      - affinity_group
    verbsDescription:
      - action: create
        method: POST
        path: /v1/affinity-groups
      - action: get
        method: GET
        path: /v1/affinity-groups/{affinity_group}
        requestFieldMapping:
          - inPath: affinity_group
            inCustomResource: spec.name
      - action: update
        method: PUT
        path: /v1/affinity-groups/{affinity_group}
        requestFieldMapping:
          - inPath: affinity_group
            inCustomResource: spec.name
      - action: delete
        method: DELETE
        path: /v1/affinity-groups/{affinity_group}
        requestFieldMapping:
          - inPath: affinity_group
            inCustomResource: spec.name
{{- end }}
