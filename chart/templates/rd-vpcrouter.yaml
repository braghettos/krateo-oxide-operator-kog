{{- if .Values.restDefinitions.vpcrouter.enabled }}
apiVersion: ogen.krateo.io/v1alpha1
kind: RestDefinition
metadata:
  name: {{ .Release.Name }}-vpcrouter
  labels:
    {{- include "krateo-oxide-operator-kog.labels" . | nindent 4 }}
  {{- if .Values.verbose }}
  annotations:
    krateo.io/connector-verbose: "true"
  {{- end }}
spec:
  oasPath: configmap://{{ .Release.Namespace }}/{{ .Release.Name }}-vpcrouter/vpcrouter.yaml
  resourceGroup: {{ .Values.resourceGroup }}
  resource:
    kind: {{ .Values.restDefinitions.vpcrouter.kind }}
    # OxideVpcRouter is addressed by its user-supplied `name` (the {router} path param); `id` is
    # server-generated/read-only. VPC-scoped via the `vpc` (and optional `project`) query param.
    identifiers:
      - name
    additionalStatusFields:
      - id
    # The {router} path placeholder would otherwise surface as a redundant spec field;
    # exclude it and source its value from spec.name via requestFieldMapping below.
    excludedSpecFields:
      - router
    verbsDescription:
      - action: create
        method: POST
        path: /v1/vpc-routers
      - action: get
        method: GET
        path: /v1/vpc-routers/{router}
        requestFieldMapping:
          - inPath: router
            inCustomResource: spec.name
      - action: update
        method: PUT
        path: /v1/vpc-routers/{router}
        requestFieldMapping:
          - inPath: router
            inCustomResource: spec.name
      - action: delete
        method: DELETE
        path: /v1/vpc-routers/{router}
        requestFieldMapping:
          - inPath: router
            inCustomResource: spec.name
{{- end }}
